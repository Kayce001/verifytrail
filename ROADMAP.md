# VerifyTrail 路线

版本：`v0.1`

## 长期目标

固定其他条件后重新运行 Tool-calling Agent，并判断它的关键业务行为是否发生危险变化。

## 第一阶段：验证 Trace

输入两条人工构造或外部采集的 JSON Trace：

```text
baseline.json
candidate.json
```

只实现：

- 检查一个事件是否发生在另一个事件之前。
- 检查一个高风险事件是否重复发生。
- 定位第一次违反规则的事件。
- 输出机器可读的验证结论。
- 提供一个“确认后才能提交”的订单 Agent 示例。

进入下一阶段前，必须能够稳定发现一次仅查看最终输出难以发现的行为退化。

## 第二阶段：录制与可控重放

只支持一个手写 Python Tool-calling Agent：

- 记录模型调用、工具调用和工具结果。
- 使用历史工具结果重新运行。
- 每次只改变模型或 Prompt。
- 验证第一阶段定义的不变量。

进入下一阶段前，必须在真实 Agent 中复现至少一次模型或 Prompt 变更导致的回归。

## 第三阶段：副作用与故障

在已有重放闭环上增加：

- 只读与写入操作分类。
- 不可逆操作默认禁止重放。
- 超时、响应丢失和部分成功。
- 重复副作用与无限重试检查。

只实现真实案例需要的故障类型。

## 第四阶段：行为比较

在积累真实 Trace 后增加：

- 去除时间戳、调用 ID 等噪声。
- 比较最终状态和副作用。
- 允许互不依赖的只读调用交换顺序。
- 定位第一次有业务意义的分叉。

行为等价和因果分析属于长期研究方向，不作为第一年必须完成的承诺。

## 范围边界

当前不做：

- 云端平台
- Dashboard
- 多租户
- Python 与 TypeScript 双 SDK
- 多框架同时适配
- 大规模 Trace 存储
- 以 LLM Judge 作为核心验证方式

每个阶段都必须先产生独立价值，再进入下一阶段。
