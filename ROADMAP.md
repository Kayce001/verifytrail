# VerifyTrail 路线

版本：`v0.1`

## 长期定位

VerifyTrail 是一个独立于模型厂商和 Agent 框架的行为验收系统。它根据用户定义的行为契约和可复核运行证据，对 AI Agent 与 Agentic Workflow 的关键行为进行验证。

```text
行为契约 + 可复核运行证据
-> VerifyTrail
-> verified / failed / unverified
```

长期定位已经确定；具体业务验证器和后续实现顺序仍需通过真实使用验证。

方向必须持续满足：

- 有明确用户价值。
- 有足够支撑一年以上投入的技术深度。
- 一个人可以逐步做成。
- 不容易退化为模型厂商或 Coding Agent 的内置测试功能。

## 原则

- 契约和证据由用户保存。
- 核心验收独立于模型厂商和 Agent 框架。
- 核心验证器不依赖具体调用入口。
- 在需要强制验收的场景中，VerifyTrail 由 Agent 外部且不可绕过的执行环境调用。
- 证据不足时输出 `unverified`，不能视为通过。
- 跨厂商是架构约束，不是同时适配多个厂商的功能承诺。

## v0.1：验证一条行为契约

输入一份行为契约和一条人工构造或外部采集的 JSON Trace：

```text
contract.json
trace.json
```

只实现：

- 定义“同一订单获得明确批准后才能提交”的行为不变量。
- 检查同一 `order_id` 的 `confirmation_received(approved=true)` 是否发生在 `submit_order` 之前。
- 定位第一次违反规则的事件。
- 输出结论、原因和违规事件位置。
- 提供 `verified`、`failed` 和 `unverified` 三个案例。

完成标准：一个本地、确定性的验证器能够对三个案例给出稳定且可复核的结果。

## 未来方向

以下内容不构成版本承诺，只在真实需求出现后选择：

- Trace 采集，用于获得运行证据。
- 可控重放，用于补充或复现实验证据。
- 行为比较，用于建立回归基线。
- 故障注入，用于产生边界条件证据。

## 范围边界

`v0.1` 不做：

- 云端平台
- Dashboard
- 多租户
- Python 与 TypeScript 双 SDK
- 厂商或框架适配器
- Trace 录制与重放
- 故障注入
- 自动修复
- 大规模 Trace 存储
- 以 LLM Judge 作为核心验证方式
- 通用行为规范 DSL
- 不含模型决策的普通确定性 Workflow
- 自动决定哪些环节应由模型处理

只有 `v0.1` 产生独立价值后，才选择下一步。
